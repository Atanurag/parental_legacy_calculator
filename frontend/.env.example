# Leave empty for local Vite development because /api is proxied to localhost:5000.
# For a deployed frontend, set this to the public Express API URL, for example:
# VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=
